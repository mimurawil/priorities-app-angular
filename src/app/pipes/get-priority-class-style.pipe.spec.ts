import { GetPriorityClassStylePipe } from './get-priority-class-style.pipe';

describe('GetPriorityClassStylePipe', () => {
  it('create an instance', () => {
    const pipe = new GetPriorityClassStylePipe();
    expect(pipe).toBeTruthy();
  });
});
