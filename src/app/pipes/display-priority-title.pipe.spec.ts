import { DisplayPriorityTitlePipe } from './display-priority-title.pipe';

describe('DisplayPriorityTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayPriorityTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
